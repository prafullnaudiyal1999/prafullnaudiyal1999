1.Install requirments.txt (pytest and selenium, webdriver-manager)
    pip install -r requirments.txt
    pip list
    pip install -U requirments.txt
    python.exe -m pip install --upgrade <package_name>
    <command> --help
    pip install --upgrade webdriver-manager
2.How to make readme.md editable?, what is learning.yaml?, convert.js?, output.json?, gitignore?, .venv?
